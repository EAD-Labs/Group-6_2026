name: Validate

on:
  pull_request:
  push:
    branches: [main]

jobs:
  repository-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check required files
        run: test -f README.md && test -f .env.example && test -f docs/BRANCHING.md
